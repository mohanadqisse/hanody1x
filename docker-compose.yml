version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: hanody-postgres
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: Aqaba2025
      POSTGRES_DB: portfolio_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data:
