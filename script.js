const fs = require('fs'); const html = fs.readFileSync(process.env.TEMP + '/uiverse.html', 'utf8'); const match = html.match(/<script id="__NEXT_DATA__" type="application\/json">(.*?)<\/script>/s); if(match){const data = JSON.parse(match[1]); const el = data.props.pageProps.element; fs.writeFileSync(process.env.TEMP + '/uiverse_html.txt', el.html); fs.writeFileSync(process.env.TEMP + '/uiverse_css.txt', el.css); console.log('success'); } else { console.log('not found');}
